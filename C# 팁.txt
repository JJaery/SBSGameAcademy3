Camera의 XToY 메소드들
X좌표(벡터)를 Y좌표로 변경해주는 메소드들

string대신 enum을 선호하라.
string을 넘겨주면 (파라미터,아규먼트) string이 복사가 일어난다. => 메모리 누수가 발생
string은 사용단에서 자유롭게 작성가능하지만 enum은 사용단에서 구현단에 구현되어 있는 것들만 사용 가능하다. => 실수 방지

switch ~ case 문에서 case문에 영역표시( {, } )는 좋다. => 같은 이름의 지역변수 사용 가능

